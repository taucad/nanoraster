---
nanoraster: patch
---

Assert worktree cleanliness when release preparation starts rather than after the quality gate has regenerated committed artifacts, so the automated release pull request can generate in CI.
