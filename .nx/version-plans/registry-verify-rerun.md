---
nanoraster: patch
---

Verify published provenance against the workflow run that minted it rather than the current retry attempt, so re-running the release verification after a transient registry failure can succeed.
