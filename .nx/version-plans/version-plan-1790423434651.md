---
__default__: patch
---

Free a renderer's retired render targets as soon as they are replaced instead of leaving them to the browser's garbage collector, which let GPU memory accumulate until a later render failed with `gpu: map_async` on WebGPU devices with a fixed memory budget
